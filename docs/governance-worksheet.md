# Governance Worksheet

| Item shared | Risk | Reason | Safer future version | Ambiguity to resolve |
|---|---|---|---|---|
| Task Tracker application source code and project structure | Low | The shared material is course toy-project code and structure, with no indicated secrets, PII, proprietary business logic, or production data. | “Use only this course-project structure and the relevant `backend/app`, `frontend`, and documentation files. Do not request or use secrets, credentials, or personal data.” | Confirm that no `.env` contents, private repository URL, or non-course code were included. |
| Feature 1 requirements: due dates and overdue filtering | Low | This is ordinary functional specification for a learning project, with no sensitive or proprietary information indicated. | “Implement this course-project requirement: optional date-only due dates, backend-owned overdue calculation, and `GET /tasks?overdue=true`. Do not access secrets or unrelated files.” | None identified from the supplied description. |
| Feature 1 Mini-ADR / architecture decisions | Low | The design rationale concerns a course project and its technical trade-offs, not customer, organizational, or regulated data. | “Use this course-project design decision as the source of truth. Restrict work to the Task Tracker codebase and exclude unrelated private information.” | Confirm the ADR did not contain private organization or deployment details outside the course project. |
| Feature 2 requirements and tag design | Low | The tag behavior is a normal functional specification for a course toy project, with no sensitive data indicated. | “Implement tags for this course project: a string list with trimming, blank rejection, bounds, filtering, and frontend chips. Do not access unrelated private data.” | None identified from the supplied description. |
| Backend source files (`models.py`, `storage.py`, `main.py`, `business_rules.py`) | Low | The supplied description identifies course-project source only and explicitly excludes `.env`; no secrets, credentials, or proprietary logic are indicated. | “Review only the named Task Tracker source files for this course assignment. Do not search for, request, or expose environment secrets, credentials, or unrelated files.” | Confirm that `.env` or any real credentials were not pasted in earlier tool sessions. |
| `README.md`, `CLAUDE.md`, user stories, verification notes, and prompt logs | Needs evidence | The row combines files whose contents may differ. The supplied material confirms the first two as course documentation but does not establish that every user-story, verification, and prompt-log entry is free of secrets, PII, or private context. | “Review only the named course documentation files after removing credentials, tokens, personal information, absolute paths, private URLs, and unrelated organizational details.” | Inspect `docs/midcourse/user-stories.md`, `verification.md`, and `prompt-log.md`, and split the row if their risk differs. |
| Terminal commands and outputs showing project paths, Git status, test results, and Claude Code installation | Medium | Absolute Windows paths, device/host naming, Git identity, and environment metadata are private context even when they are not credentials or production data. | “I ran the project locally on Windows. Here is the relevant sanitized command/result only: `[command and result]`. Remove usernames, absolute paths, tokens, environment variables, hostnames, and unrelated system details.” | Confirm whether the device-name pattern identifies an employer or organization; if so, apply stricter redaction. |
| `.github/workflows/ci.yml` requirements and CI design | Low | The described workflow is course-project CI configuration with no indicated secrets, production deployment targets, or private registry details. | “Review this course-project CI workflow. Use placeholders for credentials and repository secrets; do not expose real values.” | Confirm no hidden or referenced secret values were pasted alongside the workflow. |
| Dockerfile / Docker configuration discussed during Module 4 | Low | The described Docker configuration is a local course-project build with no indicated credentials, private registry references, or production configuration. | “Review this Task Tracker Dockerfile for correctness. Do not request or expose registry credentials, private image names, or production configuration.” | Confirm that no `docker login` output, private registry URL, or environment-file content was shared. |
| Security-review findings generated by AI | Low | Under the rubric, the findings concern a course toy project and contain no credentials, PII, production data, or unauthorized code. They may describe unpatched defects, but that does not by itself change the data-sharing classification. | “Review only these course-project security findings and cited source files. Do not request secrets, credentials, production logs, real user data, or private deployment details.” | Confirm whether the repository is public while the documented defects remain unpatched; that affects disclosure handling, though not the supplied rubric category. |
| Prompt history describing how Claude Code was used to build the Task Tracker | Needs evidence | Prompt history across all tools and sessions cannot safely be classified without checking whether any prompt included credentials, PII, private URLs, or non-course code. | “Use this sanitized course-project prompt history. Exclude credentials, tokens, PII, proprietary code, private organizational information, and absolute local paths.” | Review the complete prompt history for every tool/session before assigning Low, Medium, or High. |
