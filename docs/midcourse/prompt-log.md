# Prompt Log

## 1. Initial workflow setup
**Prompt (strong):** established the working process for this project — step-by-step execution, show exact diffs, ask permission before changes; log every prompt in `prompt-log.md`; and produce `user-stories.md`, `mini-adr.md`, `verification.md`, `reflection.md` with specific required contents.
**AI response:** acknowledged, saved the process as ongoing rules, did not touch any code/files.
**Outcome:** Accepted as-is.

## 2. Baseline scaffolding step
**Prompt (weak, as given):** "2: create that / 3: start with it but take the current command runs in the terminal which are: [raw pasted terminal transcript] / 4: start with it too but include that i rejected ur first 2 steps and edited the 3rd and fourth steps."
**Rewritten stronger version:** "Approve step 2 (create `docs/midcourse/`). Skip step 1 (branch creation) — I already created and checked out `mid-course-project` myself. For step 3, don't draft example output — use this exact terminal transcript I ran as the verification.md baseline evidence: [transcript]. For step 4, record in the prompt log that I rejected doing step 1 via AI and edited steps 3 and 4 as described."
**AI response (to the weak prompt as literally given):** created `docs/midcourse/` and drafted `verification.md`/`prompt-log.md` content for approval, using the pasted transcript verbatim and calling out the reject/edit breakdown explicitly for confirmation before saving.
**Outcome:** Edited — user supplied real command output in place of AI-drafted output (step 3) and dictated exactly what the log entry should capture (step 4); step 1 rejected outright (already done manually).

## 3. Verification.md content correction
**Prompt (strong, precise):** told AI to remove the WatchFiles auto-reload noise (two full reload cycles) from the pasted terminal transcript before it went into `verification.md`, and to restructure/decide what sections `verification.md` should actually contain rather than being just a raw log dump.
**AI response:** removed the specified reload-cycle lines from the transcript, keeping a single clean startup sequence; restructured `verification.md` into named sections (Baseline, Backend test results, Manual browser checks, Behavior contract, Break Test evidence) with the later sections left as placeholders for future feature work.
**Outcome:** Edited — user directed exactly which content to strip from the raw transcript and asked for the file to be organized into the fuller `verification.md` structure (not just the baseline transcript) up front.

## 4. Feature 1 user stories — Due Dates + Overdue Filter
**Prompt (strong):** gave the full spec for Feature 1 (due_date optional, date-only, overdue definition excluding Done tasks, backend-owned overdue logic, due date supported on create and update), plus explicit user-story format (ID, story, 2-3 acceptance criteria, notes/assumptions), a required table output, and a required approval gate before writing any file — with an explicit instruction that at least one story must capture and correct an AI assumption a human could reasonably catch.
**AI response:** inspected `models.py`/`business_rules.py`/`main.py` for current Task fields and endpoints (no code changes), then proposed 5 stories (US-D01–US-D05) in a table, covering create, update/remove, overdue determination, overdue filtering, and frontend display; listed assumptions, ambiguities (PATCH null-vs-omitted semantics for clearing due_date; `overdue=false` handling), and the AI assumptions it deliberately avoided (mandatory due dates, Done tasks counted overdue, overdue logic in JS only); stopped without creating any file.
**Outcome:** Edited then accepted — user rejected the original US-D03 wording ("...so that I can see which tasks need urgent attention") because they had not asked for that "urgent attention" framing, and required the benefit clause instead explicitly state that completed (Done) tasks are not overdue. AI rewrote US-D03 to: "...so that I can distinguish tasks that are genuinely late from tasks that are already marked Done, even if their due date has passed," keeping the acceptance criteria and notes unchanged. User then approved the full set of 5 stories as revised, and AI created `docs/midcourse/user-stories.md` with that exact wording — no other stories were rejected or further edited.

## 5. Mini-ADR approval — Feature 1 and Feature 2
**Prompt (strong):** requested a proposed mini-ADR covering both selected features (Due Dates + Overdue Filter, Tags/Labels) with a fixed structure (context, decision, alternatives, rejected-as-out-of-scope, consequences, AI-assisted decision making, implementation boundaries), specifying the approved constraints for Feature 1 (`due_date` optional and date-only `YYYY-MM-DD`; overdue computed by the backend; `overdue=true` filtering in scope; `overdue=false` explicitly out of scope; frontend must not calculate overdue itself) and for Feature 2 (tags handled later, as simple strings, not a separate entity). Explicit instruction not to create/modify any files until approved.
**AI response:** drafted the full mini-ADR content for review only; no files touched.
**Outcome:** Accepted as-is ("approved") — AI then created `docs/midcourse/mini-adr.md` with the approved content.

## 6. Feature 1 user stories — final approval
**Prompt (strong):** approved US-D01, US-D02, US-D04, and US-D05 as proposed; replaced US-D03 with a new story centered on the backend consistently identifying overdue tasks, while keeping its original acceptance criteria (ToDo/InProgress + past due date = overdue; Done = not overdue; no due date = not overdue) and keeping the note about the AI's earlier `overdue=false` assumption being corrected/rejected. Scoped the edit to only `docs/midcourse/user-stories.md`.
**AI response:** wrote `docs/midcourse/user-stories.md` with the five finalized stories, then reported the file path, full contents, and a summary.
**Outcome:** Accepted — US-D03 finalized around backend-side overdue identification; `overdue=false` filtering confirmed out of scope in both US-D03 and US-D04's notes.

## 7. Status-transition test correction (separate from Feature 1)
**Prompt:** informed AI that `test_patch_same_status_returns_422` conflicted with the intended behavior, because same-status transitions (`ToDo->ToDo`, `InProgress->InProgress`, `Done->Done`) are intentionally allowed in `VALID_TRANSITIONS`. The test itself was corrected directly on the `main` branch (commit `d7ea8ef`, "modified the test_task") to assert `200` and an unchanged status instead of `422`, and the `mid-course-project` branch was then rebased onto `main` to pick up that fix. Instructed that Feature 1 must not modify the status-transition rules.
**AI response:** this was a developer-made git change, not an AI edit — AI's role was to acknowledge it and keep Feature 1 from touching `business_rules.py`'s transition logic. Confirmed via `git log`/`git show` that the `main`-branch commit changed only the one test assertion, and that the rebase carried it cleanly into `mid-course-project`.
**Outcome:** Accepted — logged for the project timeline; Feature 1 implementation proceeded without any change to status-transition rules.

## 8. Feature 1 implementation — Due Dates + Overdue Filter
**Prompt (strong):** directed full implementation of the approved Feature 1 plan (backend: `models.py`, `business_rules.py`, `storage.py`, `main.py`; new focused tests; frontend), with explicit constraints: `due_date` optional and date-only, invalid input returns `422`, PATCH `due_date: null` clears it, overdue computed by the backend (ToDo/InProgress + past due date, Done always excluded, no due date always excluded), only `overdue=true` filtering supported, no JS-side overdue calculation, no unrelated changes, and `prompt-log.md` left untouched for this step. Required a report of files changed, a summary, and test results afterward.
**AI response:** added `due_date` and a computed `overdue` field in `models.py`; added `is_overdue()` to `business_rules.py` without touching `VALID_TRANSITIONS`/`validate_status_transition`; threaded `due_date` and an `overdue` filter through `storage.py` and `main.py`; added `backend/tests/test_due_dates.py` (13 tests); updated `frontend/index.html` with a due-date input, due-date/overdue display on cards, and an "Overdue only" filter — no overdue logic in JavaScript.
**Outcome:** Accepted — reported the exact files changed/created, a summary of the changes, and full test results as requested.

## 9. Feature 1 verification
**Prompt:** requested a verification summary covering feature-specific tests, the full backend suite, status-transition-focused tests, and confirmation that existing status-transition behavior and manual frontend behavior remained correct after Feature 1.
**AI response:** ran and confirmed: `backend/tests/test_due_dates.py` — 13 passed; full suite (`pytest tests/`) — 32 passed; status-transition-focused tests (`test_patch_valid_transition_todo_to_inprogress_returns_200`, `test_patch_invalid_transition_todo_to_done_returns_422`, `test_patch_same_status_returns_200_and_keeps_status`) — 3 passed. Frontend behavior was manually verified in the browser by the developer and confirmed working.
**Outcome:** Accepted — recorded as the closing verification entry for Feature 1; status-transition behavior confirmed unchanged by Feature 1's changes.

## 10. Feature 2 user stories — proposal
**Prompt (strong):** requested proposed user stories only (no files touched) for Feature 2 — Tags/Labels, using the approved mini-ADR as the source of truth (`tags: list[str]`, trimmed, blank rejected, bounded max count and max length, stored inline on Task, `GET /tasks?tag=...` filtering, frontend input/chips/filter, no tag entity, no colors, no autocomplete, no case-insensitive dedup). Required IDs, story text, acceptance criteria, notes/assumptions, and explicit flagging of AI assumptions needing human correction, with a stop-and-wait gate before any file changes.
**AI response:** reviewed the existing `user-stories.md` (Feature 1 format) and the approved `mini-adr.md` Tags section, then proposed 5 stories (US-T01–US-T05) covering create-with-tags, bounded count/length, PATCH replace semantics, tag filtering, and frontend input/chips/filter — explicitly flagging four unresolved assumptions for confirmation: default `[]` vs `null` when `tags` is omitted (US-T01), no numeric bound named in the ADR so proposing max 10 tags / 30 chars per tag (US-T02), full-replace vs incremental PATCH semantics (US-T03), and single-tag exact-match vs multi-tag AND/OR filtering (US-T04). Stopped without creating any file.
**Outcome:** Pending — no file created yet; assumptions carried into entry 11 for the developer's decision.

## 11. Feature 2 user stories — assumptions confirmed
**Prompt (strong):** confirmed all four flagged assumptions from entry 10 as proposed: omitted `tags` defaults to `[]` (US-T01); max 10 tags per task, max 30 characters per tag, now explicitly approved as hard limits (US-T02); PATCH `tags` uses full-replacement semantics — `tags: []` clears all, omitted `tags` preserves the existing list (US-T03); tag filtering is single-tag, exact-match, case-sensitive, no multi-tag AND/OR (US-T04). US-T05 approved as proposed. Instructed AI to update the story notes to record these as confirmed decisions rather than open assumptions, update this prompt log, and present the final story set for one more review before writing `user-stories.md`.
**AI response:** none of the four flagged assumptions were changed from what AI proposed — all were confirmed as-is, so this is a confirmation, not a correction. Rewrote the "Notes/Assumptions" sections of US-T01–US-T04 to state the decision plainly instead of asking for confirmation; added this log entry; held `user-stories.md` for final review before writing.
**Outcome:** Accepted — decisions locked in for Feature 2; `user-stories.md` not yet written, pending final review in the same turn.

## 12. Feature 2 user stories — final approval and file write
**Prompt (strong):** approved the final Feature 2 story set (US-T01–US-T05) exactly as written in entry 11, with explicit instruction (before any implementation) to: add the five stories to `docs/midcourse/user-stories.md` under a new "Feature 2 — Tags/Labels" section, and record in this prompt log the full list of AI assumptions that were corrected/confirmed for Feature 2 (omitted `tags` defaults to `[]`; max 10 tags; max 30 characters per tag; PATCH replaces the full tags list; tag filtering is single-value, exact-match, case-sensitive) — with no implementation files touched.
**AI response:** appended the "Feature 2 — Tags/Labels" section (US-T01–US-T05, unchanged from the entry-11 final text) to `docs/midcourse/user-stories.md`; added this log entry restating the five confirmed assumptions in one place per the explicit request. No implementation files (`models.py`, `storage.py`, `business_rules.py`, `main.py`, `index.html`) touched.
**Outcome:** Accepted — `user-stories.md` now contains both Feature 1 and Feature 2 stories; all five Feature 2 assumptions are confirmed decisions, not open questions. Implementation plan proposal follows in the same turn, still gated on approval before touching code.

## 13. Feature 2 implementation plan — approval with one correction
**Prompt (strong):** approved the Feature 2 implementation plan exactly as proposed, with one correction to the frontend portion: the comma-separated tag input must split and trim entries but must NOT filter out blank entries before sending — blanks must reach the backend so the existing `422` rejection (US-T01 AC4) actually fires from real UI input. Directed backend-only implementation for this step (`models.py`, `storage.py`, `main.py`, new `test_tags.py`), explicitly no changes to `business_rules.py` or `frontend/index.html`, and required exact test results reported before stopping for frontend work.
**AI response:** implemented the backend exactly per the approved plan: `MAX_TAG_COUNT`/`MAX_TAG_LENGTH` constants and `_validate_tags` in `models.py` (mirroring `_validate_title`), wired via `field_validator` on `TaskCreate`/`TaskUpdate`, plus a `tags` field on `TaskResponse`; `add_task` and `get_all_tasks(tag=...)` in `storage.py`; a `tag` query param on `GET /tasks` in `main.py`; created `backend/tests/test_tags.py` (15 tests covering trim, default-empty, blank rejection, count/length bounds at and over the limit, PATCH replace/clear/preserve, PATCH-rejects-and-leaves-unmodified, tag filter match/case-sensitivity/no-match/composability with status+priority). Did not touch `business_rules.py` or `frontend/index.html`. The frontend correction is noted for the next (frontend) implementation step — not yet acted on since frontend work hasn't started.
**Outcome:** Accepted — ran `pytest tests/test_tags.py -v` (15 passed) and the full suite `pytest tests/ -v` (47 passed, 0 failed, pre-existing deprecation warnings only, unrelated to this change). Stopped before frontend work as instructed.

## 14. Feature 2 frontend implementation
**Prompt (strong):** directed frontend-only implementation in `frontend/index.html` per the approved plan and stories: comma-separated tags input on the create/edit form, split-and-trim on submit without filtering blank entries (so backend `422` validation actually fires from real input), sent as `tags: [...]`; populate the input from `task.tags` on edit; render tags as chips on cards (no chips when empty); a single-value exact/case-sensitive tag filter via `GET /tasks?tag=...` composable with the existing overdue filter; no autocomplete/colors/tag entity/chip deletion; no backend or `business_rules.py` changes; brief report only, stop before manual browser verification.
**AI response:** added a `tags` text field to the task form (populated from `task.tags` on edit, cleared on create via existing `taskForm.reset()`); added `parseTagsInput(value)` — returns `[]` only when the whole field is empty/whitespace, otherwise splits on `,` and trims each piece without dropping blanks, so an interior blank (e.g. `"bug,,x"`) still reaches the backend and triggers `422`; wired `tags: parseTagsInput(tagsInput.value)` into the existing single `payload` object already shared by both POST and PATCH; added `renderTagChips()` for card display (empty tags → no markup) and a `.chip` style; added a `Tag` text filter next to the existing overdue checkbox, combined into `loadTasks()` via `URLSearchParams` so both filters compose. No backend files or `business_rules.py` touched.
**Outcome:** Pending manual browser verification (to be done separately per instruction) — implementation reported, not yet run in-browser by the developer.

## 15. Feature 2 browser verification report
**Prompt:** reported the completed Feature 2 manual browser checklist (14 items: create/trim/chip-display/no-chips/edit/clear/blank-422/filter-works/filter-exact-case-sensitive/filter-clear-restores/filter-composes-with-overdue/existing status-priority-overdue behavior intact) — all checked, overall result "PASSED".
**AI response:** none yet at this point — the checklist was received and held for the subsequent `verification.md` update (entry 17), per instruction not to invent/infer results and to update documentation only from what was actually reported.
**Outcome:** Accepted as the authoritative source for the Feature 2 "Manual browser checks" section of `verification.md`.

## 16. Feature 2 Break Test — performed together
**Prompt (strong):** asked to perform the Break Test together. AI proposed two candidates (break `test_create_task_with_blank_tag_returns_422` by disabling the blank-tag check in `_validate_tags`; break `test_tag_filter_is_case_sensitive` by making the tag filter case-insensitive in `get_all_tasks`), each to be broken, confirmed failing, then restored and confirmed passing. Developer approved proceeding with both as proposed.
**AI response:** executed both breaks live: (1) commented out the blank check in `backend/app/models.py`, ran the target test — failed as expected (`201` instead of `422`) — restored the check, reran — passed; (2) changed the filter comparison in `backend/app/storage.py` to lowercase both sides, ran the target test — failed as expected (task incorrectly matched by differing case) — restored exact-match comparison, reran — passed. Ran the full suite afterward (`pytest tests/ -v` → 47 passed) and grepped for leftover "BREAK TEST" markers to confirm no residual changes remained in either file.
**Outcome:** Accepted — both breaks demonstrated real test coverage (each test failed for the specific reason it claims to guard against) and both were fully reverted with no net code change; results feed directly into `verification.md`'s "Break Test evidence" section (entry 17).

## 17. verification.md updated with Feature 2 results
**Prompt (strong):** directed updating `verification.md` to document Feature 2 frontend browser verification and Break Test results, based only on what was actually performed (entries 15–16) — explicitly no invented or inferred results — while preserving the existing Feature 1 documentation and structure, and updating other docs only if the project brief explicitly requires it at this step.
**AI response:** appended two new sections to `verification.md` following the existing Feature 1 section pattern: "Feature 2 manual browser checks" (the exact 14-item checklist from entry 15, marked PASSED) and "Break Test evidence" (replacing the earlier "to be completed" placeholder) with both break/restore cycles from entry 16, plus a post-restore full-suite confirmation (47 passed). Did not modify `user-stories.md`, `mini-adr.md`, or `prompt-log.md`'s prior entries; `reflection.md` was not touched since the brief only requires it as a final wrap-up deliverable, not at this stage.
**Outcome:** Accepted — `verification.md` now contains complete Feature 1 and Feature 2 verification records, including satisfied Break Test evidence for both features' worth of coverage.